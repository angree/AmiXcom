== 0.9.0 (2026-08-21) - MUZYKA - ZROBIONE ==
- software wavetable mixer (16 glosow, 22 kHz mono) na wlasnym GM.CAT gracza
- bank sampli z FluidR3 (MIT) w data/common/music.bnk
- tryb domyslny: render raz na dysk (user/music/*.raw, ~36 MB), potem odtwarzanie
- tryb "Mixed live" + przelacznik jakosci (interpolacja), domyslny wg POMIARU maszyny
- drugi pasek postepu (bursztynowy) podczas konwersji
- naprawione: pusty options.cfg po przerwanym zapisie (cicha zmiana rulesetu)

OTWARTE PO 0.9.0
- okienko na starcie z wyborem trybu muzyki (user prosil, potem odwolal, potem
  wrocil do tematu - do ustalenia czy pokazywac zawsze przy 1. starcie)
- sprzatnac Work:aga071/aga072/aga080 (stare binarki porownawcze)
- bank 6.3 MB przy 22 kHz; 16 kHz zbilby go do ~4.6 MB i miks o 28%
- zagadka .ybc battle.cfg (zapis dziala, odczyt nie trafia; fallback ok)
- dalsze cele ladowania: rulesety 43 s, audio ~25 s, LBM/LUT ~14 s, jezyk ~13 s
- tura AI ~84 s (parkowane)

AMIXCOM - LISTA POZOSTALYCH ROBOT

ZROBIONE 20.08 (wydane jako v0.8.0):
 - Marsz bez freeze'ow: plastry FOV/swiatla na klatkach animacji (pomysl
   usera), opcja "Split movement calculation" (ON), zakladka Amiga jako
   przewijana lista. Max klatka ~60 ms (bylo ~1000 ms na granicy kratki).
 - New Battle: otwarcie 10.2->5.9 s, OK->briefing 12.7->11.4 s (szybki
   writer battle.cfg + ybc store przy OK).
NASTEPNY CEL: MUZYKA (user zaproponuje podejscie; brak MIDI na Amidze;
 platforma: Paula 2 kanaly streamowane + ADPCM 22 kHz - patrz CLAUDE.md).
OTWARTE po 0.8.0: ybc battle.cfg nie trafia przy odczycie (fallback ok);
 loading dalej: rulesety 43 s, audio ~25 s, LBM/LUT ~14 s, jezyk ~13 s;
 tura AI ~84 s (parkowane); sprzatnac Work:aga071/072/080 po porownaniach
 (falszywy alarm scrolla = amigaPerfLog:1, wyzerowany).

ZROBIONE 19.08 noc (dzien LADOWANIA; wydane jako v0.7.2):
 - Start 344 -> 128 s na 040/40 (2.7x); 030/50: 15 min -> 5.3 min.
   Kroki A-D + B + C w PROGRESS.md (slurp loaderow, hoist mapy klatek,
   cache .ybc drzew yaml, BDY row-decode, transparency-LUT lokalna paleta,
   LOFTEMPS slurp, yaml make_shared+vector, skip fontow CJK).
 - Kursor systemowy widoczny podczas splashu (mozna uzywac WB w trakcie).
POZOSTALO w ladowaniu (040/40): rulesety 43 s (konsumpcja wezlow),
 extra sounds ~13 + CATy ~12 (konwersje audio - kandydat cache/lazy),
 okno 76-77 ~14 (LBM palety + reszta LUT + TAC00.SCR), jezyk ~13, start 9.
 New Battle/bgen/load save - nadal niezmierzone po dzisiejszych zmianach
 (czesc zyska automatycznie: PCK terenu, battle.cfg NIE jest w cache ybc).

--- (stan 2026-08-17 po wydaniu 0.3.0) ---
Zasady: przed kazdym punktem pelny backup zip, jedna zmiana na build,
Claude testuje sam; pomiary czytane z oxc.log (sondy perf:/fov:/map:/step:).
UWAGA POMIAROWA: uzytkownik przelacza JIT/throttle recznie w F12 - przy
porownaniach patrzec na liczby wzgledne z tej samej sesji, nie miedzy sesjami.


ZROBIONE 18.08 wieczor - 19.08 noc (0.6.0, zbudowane, wypchniecie po tescie):
 - BITWA: LUT cieniowania + cache 8 faz animacji + brudna SIATKA KAFLI per
   faza + lista animowanych kafli w Map::animate + cache castedShade smugi
   + koncowki smugi + hidden-movement nie kasuje bufora + krok = brudne
   kafle (pelne tylko przy odslonieciu FOV / zmianie cienia) + scroll przez
   memmove buforow z pasem i propagacja. Postoj 3.8 -> 35 fps, kursor/strzal
   ~9-13, scroll 8-13, chodzenie ~6 (FOV). Szczegoly PROGRESS.md.
 - Strzal ograniczony przez _gameTimer 100 ms/krok; battleFireSpeed 6->12.
 - amigaAutoBattle (options.cfg) - boot prosto do walki. Autoinput za bramka
   Work:autoinput.on (bez pliku nigdy nie czyta skryptu).
 - Ikony WB przepisane, autostart z Work:run usuniety (run.autostart), -80%.

ZROBIONE 19.08 po poludniu (build 14:10, czeka na test):
 - TRAPV NAPRAWIONY: canTargetUnit/canTargetTile dzielily unitRadius/sqrt(0)
   gdy strzelec stoi DOKLADNIE nad/pod celem (ta sama kolumna mapy; oba
   voxele maja te same offsety +7/+8). Na PC wychodzi NaN; u nas dzielenie
   double idzie w mathieeedoubbas.library, ktorej div-by-zero WYKONUJE TRAPV
   -> Guru -> exit w srodku tury AI ("tura sie zacina"). Guard: pion -> wektor
   (radius,0). Dwa trafienia w logach, oba w turze AI, stos identyczny.
 - Sondy tury AI (JEDEN przebieg wystarczy): "aisum:" co 10 s w trakcie tury
   AI (wisząca jednostka = ostatnia linia nazywa unit id/typ) + "aiturn:
   TOTAL" po zakonczeniu: aithink ms/N, fov ms/N, path ms/N, light ms,
   ray N (calculateLine), vis hit/miss, draw ms. Zawsze wlaczone, tanie.
 - Sprzatanie logu: cale gadulstwo (prof:/geo:/cache:/us:/us2:/map:/seed:/
   sig:/frameprof/slow frame + pomiar per blit) za opcja amigaPerfLog
   (options.cfg, domyslnie 0). fov:/step: tylko >=300 ms. Wlaczenie sond =
   amigaPerfLog: 1 w options.cfg, bez rebuilda.

PILNE (19.08): tura komputera nie dziala.
 1. [x] Ekran "next turn" (kto gra + klik) byl skipNextTurnScreen:true w
    przeniesionym options.cfg -> tura AI wpada od razu w hidden movement bez
    klikniecia. Naprawione: flip w live cfg + migracja amigaCfgVersion<2
    wymusza false. UWAGA: w logu tury AI jest realny CRASH: CPU TRAP 7 (TRAPV,
    przepelnienie soft-float) w UnitWalkBState::think -> calculateFOV -> visible
    -> canTargetUnit -> distanceSq(...,bool). Handler robi exit(20) => gra ginie
    w srodku tury AI. To osobny bug (kod upstreamu), do naprawy po tescie ekranu.

STAN PO 0.7.1 (19.08 wieczor, wydane): bitwa ZAMKNIETA - rysowanie (caly
plan LEFTOFF: run-y, boxy, kafle, colour-key, scroll-faza, reachUp, asm) +
tura AI 280 -> ~84 s (pary, swiatlo, cywile, int A*, memo getTUCost) + fix
TRAPV. Postoj ~35 fps, kursor ~15, scroll 11-12. Na 030/50 walka grywalna.
NASTEPNY CEL: CZASY LADOWANIA (start ~3 min, New Battle ~35 s, bgen ~35 s,
load save ~20-25 s; sondy 6e-6v w patch scripcie gotowe). Sondy AI na stale
(aisum/aiturn); gadatliwe za amigaPerfLog (domyslnie 0).

NOWE 19.08: tura AI za wolna - zbadac sondami (LEFTOFF 1G).

PLAN RYSOWANIA BITWY 19.08 (szczegoly + liczby: LEFTOFF.md gora) - kolejnosc:
 1A run-y sprite'ow w blitNShade (C) | 1B scroll osobne boxy + kursor bez
 kolumny | 1C logika per kafel (filtr Y, puste kafle, Surface* cache,
 wskazniki setow) | 1D mapa bez colour-key | 1E leniwe przesuwanie faz |
 2 asm pętli blitu (vasm) | sprzatanie sond. Test usera po kazdym kroku.

DO ZROBIENIA - z 0.6.0:
 0. GESTA MAPA: box brudnego kafla = cala kolumna do poziomu widoku (~110 px)
    -> 15% ekranu na kazda zmiane. Ma byc: sprite kafla + 1-2 kafle NAD nim
    (izometrycznie nachodzace), obciete klipem. Nie "filtr per kafel siatki"
    (dziury+migotanie, wycofane). Po tym sufit = ui+flip ~45 ms -> ~20 fps.
 1. Sprzatanie sond: prof:/cache:/sig:/seed: + wczesniejsze.


 - GEOSCAPE ZAMARZAL 10-30 s przy ruchu myszy - NAPRAWIONE. Kazde zdarzenie
   ruchu myszy kosztowalo 370-1500 ms na maszynie z FPU (Globe::cartToPolar
   -> wersja 68881 mathieeedoub*.library) i ruchy kumulowaly sie w kolejce.
   Fix: sklejanie ruchow do najnowszej pozycji w sdlmini queue_push (ruch
   wzgledny sumowany). Zmierzone: 13 940 ms -> <20 ms na te sama serie.
 - Wersja z twardym FPU do testow: AMIGA_FPU=1 build.sh -> openxcom-aga-fpu
   (pasek "0.5.7 FPU"). Uzytkownik: w bitwie 3-4 fps tak samo jak soft,
   roznice drobne. NIE jest domyslna.
 - Ikony Workbencha przepisane (build/mkicon.py, format z portu OpenTTD,
   zero pikseli w penie 0). Autostart gry usuniety z Work:run.
 - Maszyna odniesienia: cpu_throttle -700.0 -> -800.0 (uzytkownik: -80% to
   prawdziwe 040/40). Czasy sprzed 0.5.7 byly przy -70%.

DO ZROBIENIA - zmierzone w 0.5.7, nienaprawione:
 A. New Battle (klik OK) = 9.2 s bez throttle, ~35 s u uzytkownika:
    recalcFOV 3.0 s (pelny FOV z odslanianiem kafli leci takze dla OBCYCH -
    im wystarczy nasza szybka sciezka tiles=false), MCD+PCK terenu 2.5 s
    (ladowane od nowa co bitwe, bajt po bajcie przez istream - ten sam
    loadPck co 110 s startu), initMap+initUtilities 2.5 s (14 tys.
    osobnych new Tile + tyle samo wezlow Pathfindingu).
 B. Menu glowne -> New Battle = ~17 s bez throttle, ~35 s u uzytkownika:
    NewBattleState::load() parsuje battle.cfg (27 KB gestego YAML) i buduje
    baze + 30 zolnierzy + wszystkie itemy/badania moda PRZY KAZDYM wejsciu.
    Najtansze: zapamietac zbudowany SavedGame i uzyc go ponownie.
 C. cartToPolar dalej na double - z FPU jeden ruch myszy nadal drogi, tylko
    juz sie nie kumuluje. Docelowo Q1.14 + LUT jak reszta globu.
 D. Sonda frameprof: loguje kazda klatke >=300 ms - w bitwie przy 3-4 fps
    to KAZDA klatka. Przy sprzataniu sond usunac albo podniesc prog.

1. DIRTY RECTANGLES - ZROBIONE 17.08 wieczorem (0.3.6)
   sdlmini: unia dirty + diff-blit pelnoekranowego blitu (32-bajtowe
   komorki); Screen::clear nie zeruje juz fizycznego ekranu. Menu/bitwa
   idle: 82-100/100 klatek zupelnie bez c2p. Widocznego skoku fps NIE
   dalo (c2p w loresie ~7 ms; gra i tak skladala ekran) - ale przy
   hires laced bedzie kluczowe.

2. GLOBUS - dokonczenie listy
   a) span fills oceanu/ladu (kod z 16.08: gfx_body/gfx_tex/gfx_idx.txt
      w C:\temp\amiga_oxcom + gfx_patch*.py) - lad ~15 ms -> ~1 ms
   b) cache kolek radarow (radar_patch.py) - nadal rysuja sie przy kazdym
      przerysowaniu globu (~20 ms/baza); przy stawianiu bazy co klatke
   c) sin/cos z tablic dla cachePolygons (obrot/zoom ~400 ms -> ~15 ms)
      + XuLine na intach (Bresenham)
   d) plaskie poligony ladu cieniowane wg slonca (shadepoly2.py; WYMOG:
      rozne odcienie wg kata do slonca, NIE jeden kolor, NIE tekstury;
      pokazac zrzut do akceptacji)
   e) gdy przerysowanie tanie: AMIGA_GLOBE_MIN_MS 1000 -> ~250

3. SZYBKI ZAPIS/ODCZYT GRY - GLOWNIE ZROBIONE 17.08 wieczorem (0.3.6)
   Zapis bitwy 45-60 s -> ~7.5 s; load ~90 s -> ~23 s (040/40 -70%).
   Jak: konwersje skalarne bez stringstream (yaml convert.h), small-to-
   large merge puli yaml, wlasny writer YAML (amiga_yamlout.h) zamiast
   Emittera, bezposredni zapis battleGame (saveFastAmiga, bez drzewa
   Node), gcc-ICE obejscie (-O1 -fno-inline zamiast -O0 na 14 plikach;
   Mod.cpp MUSI zostac -O0 - miscompilacja = czarne palety), leniwy
   glob (cachePolygons + _earthFix przy pierwszym uzyciu, nie w ctorze).
   POZOSTALO w loadzie (23 s): parse yaml 11 s (pomysl: reczny parser
   battleGame - format jest nasz), mapres 4.5 s, FOV/init ~3 s.
   UWAGA FORMAT: pola tileIndexSize itd. = ZNAKI ("\x04") bo loader
   czyta as<Uint8> (semantyka znaku!); binTiles w apostrofach (goly
   95 KB plain scalar = kwadratowy skaner). rng 64-bit wymaga wlasnego
   parsera (strtoul jest 32-bit).
   TRYB AUTOTESTU: Copy Work:autotest.txt -> autoinput.txt w Work:run
   (boot sam przechodzi menu->bitwa->autosave->F5->F9); powrot:
   Copy Work:run.normal Work:run.

3b. SCROLL-REUSE MAPY (warunkowe - dopiero przy hires laced)
   Przy przesuwaniu mapy bitwy przesunac blitem to co juz narysowane
   i dorysowac tylko pasek na krawedzi (poziom gry: Camera/Map).
   W lores sie nie oplaca (render 10-16 ms, c2p ~7 ms); przy 640x400
   koszt rosnie ~4x i wtedy oplaca sie na OBU poziomach - dirty rects
   w sdlmini z automatu zamienia dorysowany pasek w maly prostokat c2p.

4. BITWA - dalsze (jesli user zechce)
   - trawersa drawTerrain (petle kolumny x pietra z lancuchami getTile) -
     to wiekszosc pozostalego kosztu renderu przy cieniu >0
   - visible() (raycast wokselowy) nadal per para przy obrocie jednostki
   - ogien reakcyjny/AI tury obcych - niezmierzone porzadnie

5. PORZADKI PRZED NASTEPNYM WYDANIEM
   - usunac TYMCZASOWE sondy: perf: (sdlmini_video), globe:/blit diag
     (Globe.cpp + SDLmini_diag), slow frame (Game.cpp), step:
     (UnitWalkBState), fov: licznik (TileEngine), map: (Map.cpp)
   - klawiatura ("6"): tablica w binarce dobra; F12 w grze = screenshot
     8->24bpp = crash portu - zabezpieczyc
   - 40 s po OK w opcjach na wolnej maszynie - zmierzyc Options::save
   - RTG nietestowane; 32 MB nie laduje gry (redukcja RAM nieruszona)
   - dzwiek (Paula/ADPCM z OpenTTD) - nieruszony

GOTCHA BUILDOWE (koszt: kilka builds dzisiaj):
   Przed buildem przywracac z tarballa pliki mocno latane
   (restore_file.sh Battlescape/TileEngine.cpp itd.) - niektore laty
   nie sa idempotentne na juz zlatanym pliku (nachodza na siebie)
   i sztapluja duplikaty. build.sh clean zawsze bezpieczny.

ZROBIONE 17.08 (wydane jako 0.3.0):
- geoscape ~40 fps: glob rysuje sie tylko przy zmianie; blity colorkey
  A (4px) + B (flaga pelnych) + C (cache spanow w hwdata); usunieta
  drzemka 20 ms/klatke (SDL_Delay(1) zaokraglane do tiku)
- bitwa: krok 6 s -> ~0.3 s (FOV tylko dla ruszajacego + spotting par
  "kto widzi ruszajacego" + przyrostowe odkrywanie Fast/Accurate/Test
  + obroty bez odkrywania + addLight na intach); render mapy 100 ->
  10-16 ms (blitNShade w C, przeskok 4px, kopia przy cieniu 0);
  animacja bitwy domyslnie co 200 ms (opcja)
- zakladka AMIGA: pasek ekranu (ekran wyzszy o pasek, mysz 1:1),
  kursor amigowy (domyslny), map reveal, tempo animacji
- wersja z jednego zrodla (version.h): 0.3.0; wydania 0.1.0/0.2.0/0.3.0
  na github.com/angree/AmiXcom (kod bez assetow, release bez danych gry)

ZROBIONE 17/18.08 wieczor-noc (wydane jako 0.5.0):
- zapis bitwy 45-60 s -> ~8 s (yaml convert bez stringstream, merge puli,
  wlasny writer, saveFastAmiga bez drzewa Node, ICE-fallback -O1)
- odczyt ~90 s -> ~20-25 s (leniwy glob w GeoscapeState, earthfix.dat)
- glob 3D ~10x: geometria na intach Q14 + trig wierzcholkow raz,
  earthfix.dat z builda, cien 2x2, kolka wektorowo, XuLine 16.16,
  dogfight jednym skokiem, plaska woda cieniowana (amigaFlatGlobe)
- boot przez Run <NIL: (CLI sie domyka, WB pokazuje RAM; ~50 MB potrzebne)
- tryb autotestu: autotest.txt -> autoinput przy bootcie (run.normal = zwykly)

ZROBIONE 18.08 (wydane jako 0.5.5 i 0.5.6):
- klawiatura naprawiona (lookup rawkey miskompilowany przy -O1 - kazdy
  klawisz pisal 'r'; mapa 128 wpisow na optimize(0)) + napis PORT MADE BY
- loading splash: 6 tel z intro/ WPIECZONE w EXE (gen_splash.py ->
  amiga_splash_data.c), logo -2px, fade paleta, pasek liniowy do czasu
  (ticki w calym Mod loading + hak YamlTickHook w yaml Stream::get),
  peny Intuition na indeksach 0/15/17/19, paleta czerniona od OpenScreen
- POMIAR STARTU (040/40 -70%, ~180 s do menu): rulesety ~60 s,
  vanilla+battlescape resources (SCR/PCK/CAT) ~110 s <- GLOWNY CEL
  przyspieszenia startu; jezyk ~20 s; zagadka: ~5.5 s/region sanityzacji
- sondy TEMP do sprzatniecia rozszerzone o "splash:"
